# AI-PDF-summarizer
This is a PDF summarizer which is powered by  LangGraph. When a PDF is added to the source folder it automatically creates a summarized pdf for that into the summary_notes directory inside the root directory. If the source pdf is an image-based one the text is extracted through the tesseract OCR programme. You have to install tesseract locally.
